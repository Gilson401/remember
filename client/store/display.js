export const state = () => {
    return {
        mode: 'quest',
    }
}

export const mutations = {
    SET_MODE(state, mode) {
        state.mode = mode
    },
}

export const actions = {

}

export const getters = {

}